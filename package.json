{
  "name": "weather",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run --filter='*' dev",
    "build": "bun run --filter=ui build",
    "start": "bun run --filter=server start",
    "lint": "bun run --filter=ui lint"
  }
}
